# 神之心

> 隶属：[神圣规划](../aimon.md) / 基础层

**定位**：独立的 LLM 资源池，与世界树平级，不归属于任何上层模块。

## 核心能力

- **浅层池**：轻量 LLM，支撑天使体系 + 派蒙闲聊
- **深层池**：重型 LLM，支撑四影-七神的深度业务
- **非抢占式调度**：简单任务不中断复杂任务
- **负载均衡 + 故障转移**：LLM 实例自动切换
- **分层标准**：待定（按参数量？按 provider？）

## 与空执的边界

神之心管 **LLM 实例**故障（模型调不通 / 超时就换实例）；**七神业务实例**故障归空执。
