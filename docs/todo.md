# 待办项 / 下一步

> 隶属：[神圣规划](aimon.md)

记录本架构尚待展开的细节 / 技术选型 / 迁移工作。后续每项完成后可转为对应模块的专项子文档。

## 1. 职能层面

- [ ] 派蒙闲聊响应的具体交互形态（复用主会话 session？独立的轻量 session？）
- [ ] 冰神 **AI 自举生成 skill** 的可行性验证（参考 agent harness 自升级迭代，如 Anthropic "Claude can write its own tools" 思路）
- [ ] **推送具体策略**（三段式路径已定，细节待定）：推送时机 / 频率 / UX 形态 / 打断策略 / 积压处理 / 事件响铃的优先级仲裁

## 2. 技术选型层面

- [x] ~~**地脉**实现：asyncio.Queue / Redis Stream / 自研？~~ —— 2026-04-22 已选定 asyncio.Queue 进程内发布/订阅，见 `paimon/foundation/leyline.py`
- [ ] **神之心**分层标准：参数量 / provider / 成本？
- [ ] **时执**上下文压缩阈值（3.5k 偏低，需调研主流方案）
- [ ] **生执**依赖环回滚的具体机制（saga 补偿 / 状态快照）
- [x] ~~**时执**归档存储介质：本地 SQLite / 独立归档库 / 对象存储？~~ —— 2026-04-22 架构升级后已解决：归档与活跃同库在世界树内，靠生命周期字段（热/冷/过期）区分
- [ ] **异常日志**落盘方案（独立日志设施，不入世界树）
- [ ] **Skill / Tool manifest** 的 `sensitivity` 字段设计（改 [fairy/skills/parser.py](../fairy/skills/parser.py) + [fairy/tools/base.py](../fairy/tools/base.py)）
- [ ] **永久授权**在世界树里的存储结构（条目粒度：按 skill / 按工具 / 按参数模式？）
- [ ] **用户答复**的交互形态（文本识别 / 按钮 / 默认超时如何处理）
- [ ] **原石多维度标签**的 schema 设计（`module` + `purpose` 枚举集）
- [ ] **草神面板撤销 → 派蒙缓存同步**机制（面板撤销永久授权后，派蒙本地缓存如何失效 / 重读）

## 3. 迁移层面

- [ ] 三省六部（旧架构）→ 神圣规划 的职能迁移映射表
- [ ] 现有 channels（QQ / TG / WebUI）的接入点调整
- [ ] 现有 workflow 引擎如何退役 / 改造为生执
